//Count the number of <li> in the countItems.html file and show the results in console.

let liEl = document.querySelectorAll("li");

let Count = `the number of <li> is ${liEl.length}`;
console.log(Count);

// count = 0;
// liEl.forEach((element) => count++);
// console.log(count);
