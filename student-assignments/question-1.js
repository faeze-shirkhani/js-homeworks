/*
  todo1: what is the difference between an asynchronous function and a synchronous function?
*/

// ! Answer


// When we write synchronous code, it runs one line at a time in order. Each line waits for the previous one to finish before it starts. This is useful for simple calculations or step-by-step logic.

// But with asynchronous code, we can start a task and move on without waiting for it to finish. It does not block the rest of the code. While the task is still running (like waiting for data), the program can continue doing other things.