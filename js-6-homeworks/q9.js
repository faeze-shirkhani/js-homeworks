/** @format */
//add comments
// explain what this function does. you can use Google!

function whatDoIDo() {
  return Math.random() * 8 + 1;
}
// explain what does this function do, google it
function whatICanDo(word1, word2) {
  return word1.concat(word2);
}

// The whatDoIDo() function generates a random decimal number between 1 and 9 (excluding 9) by multiplying Math.random() by 8 and adding 1. The whatICanDo(word1, word2) function concatenates two input strings using the .concat() method and returns the combined result.
